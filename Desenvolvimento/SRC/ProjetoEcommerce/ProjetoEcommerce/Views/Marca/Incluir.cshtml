@model ProjetoEcommerce.Dominio.Entidades.Marca

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.ShowMenu = true;
}

@using (Html.BeginForm("IncluirConfirm", "Marca", FormMethod.Post))
{
    @Html.HiddenFor(model => model.MarcaID)
    @Html.HiddenFor(model => model.CriadoEm)
    @Html.HiddenFor(model => model.Status)
    <div class="form-group">
        <label for="inputAddress">Nome</label>
        @Html.TextBoxFor(modelItem => Model.Nome, new { @class = "form-control", @placeholder = "Digite o nome da Marca" })
    </div>
    <input type="submit" value="SALVAR" class="btn btn-success btn-block" />
}
