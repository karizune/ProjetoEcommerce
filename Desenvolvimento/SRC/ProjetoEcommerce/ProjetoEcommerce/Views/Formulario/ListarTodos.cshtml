@using ProjetoEcommerce.Dominio.Entidades.Seguranca
@model System.Collections.Generic.IEnumerable<Formulario>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.ShowMenu = true;
    ViewBag.Title = "Manutenção de Formulários";
}

<div class="admin-titulo-principal">
    @ViewBag.Title
    <a class="btn btn-success btn-sm float-right" href="@Url.Action("Incluir", "Formulario")">
        <i class="fa fa-plus"></i>
        INCLUIR
    </a>
</div>

<table class="table table-striped">
    <thead>
        <tr>
            <td>ID</td>
            <td>Descrição</td>
            <td>URL</td>
            <td>Status</td>
            <td>Usuário</td>
            <td>Criado Em</td>
            <td>Atualizado Em</td>
            <td></td>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.FormularioID</td>
                <td>@item.Descricao</td>
                <td>@item.URL</td>
                <td>@item.Status</td>
                <td>@item.Usuario</td>
                <td>@item.CriadoEm</td>
                <td>@item.AtualizadoEm</td>
                <td>
                    <a class="btnDelete" href="#" onclick="myFunction('@Url.Action("Delete","Formulario", new { FormID = item.FormularioID})')">Deletar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
