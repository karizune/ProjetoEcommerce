@model System.Collections.Generic.IEnumerable<ProjetoEcommerce.Dominio.Entidades.Categoria>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.ShowMenu = true;
    ViewBag.Title = "Manutenção de Tipos de Cartão";
}

<div class="admin-titulo-principal">
    @ViewBag.Title
    <a class="btn btn-success btn-sm float-right" href="@Url.Action("Incluir", "Categoria")">
        <i class="fa fa-plus"></i>
        INCLUIR
    </a>
</div>


<table class="table table-hover table-sm">
    <thead class="thead-dark">
        <tr>
            <th>Nome</th>
            <th>Criado Em</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Nome</td>
                <td>@item.CriadoEm</td>
                <td>
                    <a href="#" class="btn btn-warning btn-sm">
                        <i class="fa fa-edit"></i>
                    </a>
                    <a href="@Url.Action("Excluir", "Categoria", new { id = item.CategoriaID })" class="btn btn-danger btn-sm">
                        <i class="fa fa-remove"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
